import { BoardSizeConfigs } from './board-types';

export const BOARD_SIZE_CONFIGS: BoardSizeConfigs = {
  small: {
    rows: 30,
    columns: 50,
  },
  medium: {
    rows: 50,
    columns: 70,
  },
  large: {
    rows: 80,
    columns: 100,
  },
};
