import { BoardSizeConfigs } from '@/components/board';

export const BOARD_SIZE_CONFIGS: BoardSizeConfigs = {
  small: {
    rows: 30,
    columns: 50,
  },
  medium: {
    rows: 50,
    columns: 70,
  },
  large: {
    rows: 80,
    columns: 100,
  },
};

export const SIM_SPEED = {
  slow: '',
  medium: '',
  fast: '',
};
