export enum EBoardSize {
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
}

export enum ESimSpeed {
  Slow = 'slow',
  Fast = 'fast',
}

export enum ERandomPercentage {
  None = 'none',
  Small = 'small',
  Middle = 'middle',
  High = 'high',
}

export enum ESettingsConfigItem {
  BoardSizeConfig = 'boardSizeConfig',
  SimSpeedConfig = 'simSpeedConfig',
  RandomFillPersentageConfig = 'randomFillPersentageConfig',
}
